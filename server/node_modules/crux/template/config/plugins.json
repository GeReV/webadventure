// Plugin config file

{
	// Adds user-agent parsing the handle object
	"useragent": true,
	
	// Adds cookie handling to the handle object
	"cookies": true,
	
	// Automatically opens a session for every request
	"sessions": false
}

// End of file plugins.json
// Location: ./config/plugins.json
