
$.libs.require('sessions');

$.plugins.bind('request', function(done, handle) {
	$.libs.sessions.open(handle, function(err) {
		if (err) {
			throw handle.httpError(500, err);
		}
		process.nextTick(done);
	});
});

